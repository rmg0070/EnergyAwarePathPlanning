
                # temp =  test_X.reshape(len(x_vals),len(y_vals),2)
                

                # for i, x in enumerate(x_vals):
                #     for j, y in enumerate(y_vals):
                #         # print(X[i, j])
                #         if X[i, j] == sampling_goal[0,robot] and Y[i,j] ==  sampling_goal[1,robot] :
                #             # print(X[i, j],Y[i,j])
                #             found_index=[i,j]
                #             # print(f"found index:{robot}{found_index}")
                #             # print(x,y)
                #             # print(sampling_goal[:,robot])
                #             break
                #     # if found_index:
                #     #     break
                # print(f"{points_grid_x[found_index[0],found_index[1]]}")
                # points_grid_x[found_index[0],found_index[1]] += 1
                # print(f"{points_grid_x[found_index[0],found_index[1]]}")
                # print(f"count value at {points_grid_x[found_index[0],found_index[1]]}")
                # for ind1 in range(len(x_vals)):
                #     for ind2 in range(len(y_vals)):
                #         # print(f"idx1:{ind1} , idx2:{ind2}")
                #         # print(f"tempx{temp[ind1,ind2,0]},tempy:{temp[ind1,ind2,1]}")
                #         # print(f"x:{sampling_goal[0,robot]} y:{sampling_goal[1,robot]}")
                #         if sampling_goal[0,robot] == X[ind1][ind2] and sampling_goal[1,robot] == Y[ind2]:
                #             found_index=[ind1,ind2]
                #             break
                    
                #     if found_index:
                #         break
                # print(f"found index:{robot}{found_index}")
                # points_grid_x[found_index[0],found_index[1]] = points_grid_x[found_index[0],found_index[1]]+1
        # print(points_grid_x)